{
    "type": "object",
    "properties": {
        "appId": {
            "required": true,
            "type": "string",
            "format": "uuid"
        },
        "versionId": {
            "required": true,
            "type": "string"
        },
        "entityId": {
            "required": true,
            "type": "string",
            "format": "uuid"
        },
        "requestBody": {
            "description": "Object model for creating an explicit list item.",
            "properties": {
                "explicitListItem": {
                    "description": "The explicit list item.",
                    "type": "string"
                }
            },
            "type": "object"
        }
    }
}